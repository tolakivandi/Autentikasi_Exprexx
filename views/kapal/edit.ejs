<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Document</title>
</head>

<body>
    <div class="container">
        <h1 class="text-center mb-5">Data Kapal</h1>
        <form action="/kapal/update/<%= id %>" method="POST">
            <div class="mb-3">
                <label for="nama" class="form-label">Nama Kapal</label>
                <input type="text" class="form-control" name="nama_kapal" value="<%= nama_kapal %>"  required>
            </div>
            <div class="mb-3">
                <label for="nama" class="form-label">nama pemilik</label>
                <select class="form-select" aria-label="Pilih Tingkat Keahlian" name="id_pemilik" required>
                    <% for(var i=0; i < data_pemilik.length; i++){ %>
                        <% if(data_pemilik[i].id_pemilik === id_pemilik){ %>
                            <option value="<%= data_pemilik[i].id_pemilik %>" selected><%= data_pemilik[i].nama_pemilik %></option>
                        <% } else { %>
                            <option value="<%= data_pemilik[i].id_pemilik %>"><%= data_pemilik[i].nama_pemilik %></option>
                        <% } %>
                    <% } %>
                </select>
            </div>            
            <div class="mb-3">
                <label for="nama" class="form-label">nama dpi</label>
                <select class="form-select" aria-label="Pilih Tingkat Keahlian" name="id_dpi" required>
                    <% for(var i=0; i < data_dpi.length; i++){ %>
                        <% if(data_dpi[i].id_dpi === id_dpi){ %>
                            <option value="<%= data_dpi[i].id_dpi %>" selected><%= data_dpi[i].nama_dpi %></option>
                        <% } else { %>
                            <option value="<%= data_dpi[i].id_dpi %>"><%= data_dpi[i].nama_dpi %></option>
                        <% } %>
                    <% } %>
                </select>
            </div>     
            <div class="mb-3">
                <label for="nama" class="form-label">Nama Alat Tangkap</label>
                <select class="form-select" aria-label="Pilih Tingkat Keahlian" name="id_alat" required>
                    <% for(var i=0; i < data_alat.length; i++){ %>
                        <% if(data_alat[i].id_alat === id_alat){ %>
                            <option value="<%= data_alat[i].id_alat %>" selected><%= data_alat[i].nama_alat %></option>
                        <% } else { %>
                            <option value="<%= data_alat[i].id_alat %>"><%= data_alat[i].nama_alat %></option>
                        <% } %>
                    <% } %>
                </select>
            </div>
            
            <div class="mb-3">
                <button type="submit" class="btn btn-sm btn-success">Simpan</button>
            </div>
        </form>
    </div>
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>

</html>